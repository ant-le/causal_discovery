# @package data

defaults:
  - _base
  - _self_

# Smoke-test overrides: fewer seeds, smaller batches, fast iteration.
train_family:
  name: bcnp_all_data_train_smoke

seeds_val: [1000]
seeds_test: [2000]
num_workers: 0
pin_memory: false
batch_size_train: 8

# @package inference

n_samples: 20
auc_num_shuffles: 1000
auc_balance_classes: true
auc_seed: 0
use_cached_inference: true
cache_inference: true

cache_compress: true
cache_dtype: "uint8"
cache_n_samples: 50

# DiBS/BayesDAG use their full model-level defaults from
# model/dibs.yaml and model/bayesdag.yaml (no overrides here).

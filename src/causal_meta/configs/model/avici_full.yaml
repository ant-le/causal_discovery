# @package model

type: "avici"
id: "avici_full"

# Large transformer (paper-scale).
d_model: 512
dim_feedforward: 2048
nhead: 16
num_layers: 4
dropout: 0.1
emb_depth: 2
use_positional_encoding: false

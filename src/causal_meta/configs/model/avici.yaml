# @package model

type: "avici"
id: "avici"

# Reference AVICI backbone defaults from the upstream repository.
dim: 128
layers: 8
key_size: 32
num_heads: 8
widening_factor: 4
dropout: 0.1
logit_bias_init: -3.0
cosine_temp_init: 0.0
mask_diag: true

# @package model

type: "avici"
id: "avici"

# RQ1-sized transformer (section 4.2 scale).
d_model: 512
dim_feedforward: 1024
nhead: 8
num_layers: 4
dropout: 0.1
emb_depth: 2
use_positional_encoding: false
